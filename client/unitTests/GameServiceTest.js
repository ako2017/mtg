QUnit.test('on pose un terrain pour la 1ere fois', function(assert) {
	assert.ok(false);
});

QUnit.test('on pose un terrain dans le meme tour pour la 2nd fois on ne peut pas', function(assert) {
	assert.ok(false);
});

QUnit.test('on pose une creature', function(assert) {
	assert.ok(false);
});

QUnit.test('on pose une 2nd creature dans la pile interdit', function(assert) {
	assert.ok(false);
});

QUnit.test('on fait un muligane en phase de distribution', function(assert) {
	assert.ok(false);
});

QUnit.test('on fait un muligane en phase autre que distribution', function(assert) {
	assert.ok(false);
});

